---
phase: 05-web-dashboard
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: []
autonomous: false
requirements: [DEL-01]

must_haves:
  truths:
    - "Dashboard loads in browser without console errors"
    - "Signal card shows correct recommendation with proper color coding"
    - "Dealer price table shows realistic prices for all 5 dealers"
    - "Price charts render with data points for all 3 product types"
    - "Gap chart renders with historical gap and moving average lines"
    - "Mode toggle switches between Saver and Trader signal display"
    - "HTMX auto-refresh updates prices and signal after 30 seconds"
    - "Mobile viewport (375px) shows all sections without horizontal scrolling"
    - "Dashboard looks professional with consistent gold/dark theme"
  artifacts: []
  key_links: []
---

<objective>
Human verification that the complete dashboard works end-to-end in a browser — visual correctness, mobile responsiveness, interactive features, and live data refresh.

Purpose: Confirm that the dashboard delivers the full user experience described in DEL-01 before marking Phase 5 complete.

Output: User approval (or bug reports for gap closure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-dashboard/05-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify dashboard in browser</name>
  <files>(no files modified — verification only)</files>
  <action>Human verifies the complete dashboard in browser following the how-to-verify checklist below. No code changes needed unless issues are found.</action>
  <verify>
    <automated>python -m pytest tests/ -x — all existing tests still pass before human verification</automated>
  </verify>
  <done>User confirms "approved" after testing all 14 checklist items in the browser</done>
  <what-built>
    Complete Gold Advisor Vietnam web dashboard at http://localhost:8000/ with:
    - Signal card showing Buy/Hold/Sell recommendation with confidence bar and reasoning
    - Dealer price table with 5 dealers (SJC, Doji, PNJ, BTMC, Phú Quý) showing SJC bar and ring gold buy/sell prices
    - Gap display showing SJC-international gap in VND and percentage with trend indicator
    - Price chart (Chart.js line chart) with SJC bar, ring gold, and international gold across selectable timeframes (1D/1W/1M/1Y)
    - Gap chart showing historical gap percentage with 7d/30d moving averages
    - Saver/Trader mode toggle
    - HTMX auto-refresh every 30 seconds for prices and signal
    - Mobile-responsive layout with gold/dark theme
  </what-built>
  <how-to-verify>
    1. Start the server: `uvicorn src.api.main:app --reload --port 8000`
    2. Open http://localhost:8000/ in Chrome/Firefox
    3. Verify signal card shows recommendation badge (BUY/HOLD/SELL) with correct color (green/amber/red), confidence bar fills to correct percentage, reasoning text is readable
    4. Verify dealer price table shows all 5 dealers with SJC bar and ring gold buy/sell columns, spread values, and last updated timestamps
    5. Verify gap display shows gap value in VND with percentage and trend arrow
    6. Click each timeframe button on price chart (1D, 1W, 1M, 1Y) — chart should reload with new data
    7. Click each timeframe button on gap chart (1W, 1M, 3M, 1Y) — chart should reload
    8. Click "Trader" mode toggle — signal card should update to show Trader-mode signal
    9. Click "Saver" mode toggle — signal should switch back
    10. Wait 30+ seconds — verify price table and signal card auto-refresh
    11. Open Chrome DevTools Console (F12) — verify no JavaScript errors
    12. Open DevTools Network tab — verify HTMX requests fire and return 200
    13. Resize browser to 375px width (mobile) — verify layout stacks vertically, no horizontal scroll
    14. Check visual polish: consistent gold accent color, readable text, professional appearance
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks good, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- All items in the how-to-verify checklist pass
- User confirms "approved"
</verification>

<success_criteria>
- User has tested dashboard in browser and approved the visual/functional result
- Any issues identified have been noted for gap closure
</success_criteria>

<output>
After approval, create `.planning/phases/05-web-dashboard/05-03-SUMMARY.md`
</output>
