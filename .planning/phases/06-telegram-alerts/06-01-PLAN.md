---
phase: 06-telegram-alerts
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/alerts/bot.py
  - src/alerts/__init__.py
  - src/config.py
  - tests/test_bot.py
  - pyproject.toml
autonomous: true
requirements: [DEL-02]

must_haves:
  truths:
    - "Telegram bot starts and responds to /start command"
    - "Telegram bot responds to /status command with current signal info"
    - "Bot token is read from TELEGRAM_BOT_TOKEN env var"
    - "Bot gracefully handles missing token (logs error, does not crash)"
    - "Bot runs in background thread, not blocking the FastAPI event loop"
  artifacts:
    - path: "src/alerts/bot.py"
      provides: "Telegram bot with /start and /status commands"
      exports: ["start_bot()", "stop_bot()"]
      min_lines: 60
    - path: "src/alerts/__init__.py"
      provides: "Package init for alerts module"
    - path: "tests/test_bot.py"
      provides: "Unit tests for bot command handlers"
  key_links:
    - from: "src/alerts/bot.py"
      to: "src.engine.pipeline.compute_signal"
      via: "asyncio.to_thread"
      pattern: "compute_signal"
    - from: "src/alerts/bot.py"
      to: "src.config.Settings"
      via: "Settings().telegram_bot_token"
      pattern: "telegram_bot_token"
---

<objective>
Create the Telegram bot foundation with /start and /status commands. The bot runs in a background thread alongside the FastAPI server and APScheduler, providing the messaging infrastructure that subsequent alert plans build upon.

Purpose: Establish bot lifecycle management (start/stop), command handlers, and integration hooks for the scheduler pipeline.

Output: Telegram bot module with /start (welcome + chat_id registration) and /status (current signal display) commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From src/engine/types.py:
```python
@dataclass
class Signal:
    recommendation: Recommendation  # BUY, HOLD, SELL (StrEnum)
    confidence: int                 # 0-100
    factors: list[SignalFactor]
    reasoning: str
    mode: SignalMode               # SAVER, TRADER
    timestamp: datetime
    gap_vnd: float | None
    gap_pct: float | None
```

From src/engine/pipeline.py:
```python
def compute_signal(db_path: str, mode: SignalMode = SignalMode.SAVER) -> Signal
```

From src/config.py:
```python
class Settings(BaseSettings):
    database_url: str = "sqlite+aiosqlite:///./gold_advisor.db"
    log_level: str = "INFO"
    fetch_interval_minutes: int = 5
    freshness_threshold_minutes: int = 15
    anomaly_threshold_percent: float = 10.0
```

From python-telegram-bot 22.x:
- `ApplicationBuilder().token(TOKEN).build()`
- `Application.run_polling()` for background operation
- `CommandHandler("start", start_handler)` for command registration
- `Update.effective_chat.id` to get chat_id
- `update.message.reply_text()` to send messages
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Telegram bot module with /start and /status commands</name>
  <files>src/alerts/__init__.py, src/alerts/bot.py, src/config.py, pyproject.toml, tests/test_bot.py</files>
  <behavior>
    - /start command responds with welcome message in Vietnamese + English: "Gold Advisor Vietnam Bot\n\nUse /status to check current signal.\n\nLưu ý: Đây là thông tin thị trường, không phải tư vấn đầu tư. (Market information only, not financial advice.)"
    - /start command logs the chat_id for future alert dispatch
    - /status command calls compute_signal() and responds with formatted signal: recommendation, confidence, reasoning, gap info
    - /status handles insufficient data gracefully (returns "Chưa đủ dữ liệu" / "Insufficient data" message)
    - Bot token comes from Settings.telegram_bot_token (new field)
    - Missing/empty token: bot does not start, logs warning
    - start_bot() and stop_bot() functions for lifecycle management
    - Bot runs via Application.run_polling() in a separate thread (not blocking FastAPI)
  </behavior>
  <action>
    1. **Add dependency** to pyproject.toml: `python-telegram-bot>=22.0`

    2. **Add config field** to src/config.py:
       - `telegram_bot_token: str = ""`  (empty = disabled)

    3. **Create `src/alerts/__init__.py`** — empty package init

    4. **Create `src/alerts/bot.py`** with:
       - `SUBSCRIBED_CHATS: set[int] = set()` — stores chat_ids from /start
       - `_application: Application | None = None` — module-level bot reference
       - `_thread: Thread | None = None` — background thread reference
       - `_db_path: str` — module-level database path
       - `async def start_handler(update, context)` — /start command handler
         - Get chat_id from update.effective_chat.id
         - Add to SUBSCRIBED_CHATS
         - Reply with welcome message (Vietnamese + English + disclaimer)
       - `async def status_handler(update, context)` — /status command handler
         - Call compute_signal via asyncio.to_thread with _db_path
         - Format signal into readable message
         - Handle insufficient data (0 confidence HOLD)
       - `def _format_signal_message(signal: Signal) -> str` — format signal for Telegram
         - Include emoji badges: BUY=🟢, HOLD=🟡, SELL=🔴
         - Include confidence as percentage
         - Include reasoning
         - Include gap info if available
         - Include disclaimer line
       - `def start_bot(db_path: str) -> None` — start bot in background thread
         - Read token from Settings
         - If token empty, log warning and return
         - Build Application with token
         - Register /start and /status handlers
         - Store db_path in module var
         - Start polling in daemon thread
       - `def stop_bot() -> None` — stop bot gracefully
         - Call application.stop() if running
         - Join thread with timeout

    5. **Create `tests/test_bot.py`** — unit tests for handlers:
       - Use `python-telegram-bot` testing utilities: `ApplicationBuilder().token("test").build()`
       - Test start_handler: adds chat_id to SUBSCRIBED_CHATS, replies with welcome
       - Test status_handler: calls compute_signal, formats response
       - Test status_handler with insufficient data: returns graceful message
       - Test format_signal_message: correct formatting for BUY/HOLD/SELL
       - Test start_bot with empty token: no crash, no thread started
       - Test start_bot with valid token: creates thread (mock run_polling)

    TDD: Write tests first (RED), then implement (GREEN).

    Commits:
    - `test(06-01): add failing tests for Telegram bot handlers`
    - `feat(06-01): implement Telegram bot with /start and /status commands`
  </action>
  <verify>
    <automated>python -m pytest tests/test_bot.py -x -v</automated>
  </verify>
  <done>
    - /start handler registers chat_id and sends welcome message
    - /status handler returns formatted signal with disclaimer
    - Bot handles missing token gracefully
    - start_bot() / stop_bot() lifecycle functions work
    - All existing tests still pass
    - 2 atomic TDD commits
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_bot.py -x -v` — all bot tests pass
- `python -m pytest tests/ -x` — all project tests pass (existing + new)
- Import check: `python -c "from src.alerts.bot import start_bot, stop_bot, SUBSCRIBED_CHATS"`
</verification>

<success_criteria>
- Telegram bot module created with /start and /status command handlers
- Bot lifecycle (start/stop) works via background thread
- Missing token handled gracefully (no crash)
- Signal formatting includes emoji badges, confidence, reasoning, disclaimer
- SUBSCRIBED_CHATS set populated from /start for future alert dispatch
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-telegram-alerts/06-01-SUMMARY.md`
</output>
