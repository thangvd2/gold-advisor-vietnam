---
phase: 05-web-dashboard
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - templates/dashboard.html
  - templates/partials/signal_card.html
  - templates/partials/price_table.html
  - templates/partials/gap_display.html
  - templates/partials/price_chart.html
  - templates/partials/gap_chart.html
  - static/js/charts.js
  - static/js/dashboard.js
  - src/api/routes/dashboard.py
  - tests/test_dashboard_html.py
autonomous: true
requirements: [DEL-01]

must_haves:
  truths:
    - "Dashboard displays signal card with Buy/Hold/Sell recommendation, confidence bar, and reasoning text"
    - "Dashboard displays dealer price table with all dealers showing SJC bar and ring gold buy/sell prices"
    - "Dashboard shows SJC-international gap value with percentage and trend indicator"
    - "Dashboard includes price chart (line) for SJC bars, ring gold, and international gold"
    - "Dashboard includes gap chart showing historical gap trend with moving averages"
    - "User can select Saver or Trader mode toggle and signal updates accordingly"
    - "Price table and signal card auto-refresh every 30 seconds via HTMX"
    - "Dashboard is mobile-responsive — readable on 375px width without horizontal scroll"
  artifacts:
    - path: "templates/dashboard.html"
      provides: "Main dashboard page layout with all sections"
      min_lines: 80
    - path: "templates/partials/signal_card.html"
      provides: "Signal display section with recommendation, confidence, reasoning"
      min_lines: 30
    - path: "templates/partials/price_table.html"
      provides: "Dealer price table with all sources and product types"
      min_lines: 40
    - path: "templates/partials/gap_display.html"
      provides: "Gap value and trend indicator"
      min_lines: 20
    - path: "static/js/charts.js"
      provides: "Chart.js configuration for price and gap charts"
      min_lines: 80
    - path: "static/js/dashboard.js"
      provides: "HTMX refresh logic and mode toggle handler"
      min_lines: 30
  key_links:
    - from: "templates/dashboard.html"
      to: "/dashboard/prices"
      via: "HTMX hx-get on page load and polling"
      pattern: "hx-get.*dashboard/prices"
    - from: "templates/dashboard.html"
      to: "/dashboard/signal"
      via: "HTMX hx-get with mode query param"
      pattern: "hx-get.*dashboard/signal"
    - from: "static/js/charts.js"
      to: "/api/prices/history"
      via: "fetch() JSON API call"
      pattern: "fetch.*api/prices/history"
    - from: "static/js/charts.js"
      to: "/api/gap/history"
      via: "fetch() JSON API call"
      pattern: "fetch.*api/gap/history"
    - from: "static/js/dashboard.js"
      to: "templates/partials/signal_card.html"
      via: "HTMX partial swap on interval"
      pattern: "hx-trigger.*every"
---

<objective>
Build the complete dashboard UI with all data sections — signal card, dealer price table, gap display, price charts, gap chart — using Jinja2 templates, Chart.js for visualizations, and HTMX for live data refresh.

Purpose: Deliver the full user-facing dashboard that surfaces all analytical outputs from Phases 1-4 in a mobile-responsive layout.

Output: Complete dashboard page with signal card, dealer prices, gap tracker, price/gap charts, mode toggle, and HTMX auto-refresh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-dashboard/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/api/routes/dashboard.py (created in 05-01):
```python
# GET /dashboard/prices
# Response: {"dealers": [{"source": "DOJI", "products": [...], "fetched_at": "..."}, ...]}

# GET /dashboard/signal?mode=saver
# Response: {"recommendation": "BUY", "confidence": 75, "reasoning": "...", "mode": "saver", "gap_vnd": ..., "gap_pct": ...}
```

From existing API endpoints (from Phases 1-4):
```python
# GET /api/prices/history?product_type=sjc_bar|ring_gold|xau_usd&range=1D|1W|1M|1Y
# Response: {"product_type": "sjc_bar", "range": "1M", "prices": [{"x": "ISO-timestamp", "y": float}, ...]}

# GET /api/gap/history?range=1W|1M|3M|1Y
# Response: {"range": "1W", "gaps": [{"timestamp": "...", "gap_vnd": float, "gap_pct": float, "ma_7d": float|null, "ma_30d": float|null}, ...]}

# GET /api/gap/current
# Response: {"gap": {"gap_vnd": float, "gap_pct": float, "sjc_avg_vnd": float, "xau_vnd": float, "ma_7d": float|null, "ma_30d": float|null}}
```

From templates/base.html (created in 05-01):
- Block structure: {% block title %}, {% block content %}, {% block scripts %}
- Extends base.html pattern: `{% extends "base.html" %}`
- CDN loaded: Tailwind, Chart.js, HTMX
- Gold/dark theme: --gold: #D4AF37
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dashboard HTML sections — signal card, price table, gap display</name>
  <files>templates/dashboard.html, templates/partials/signal_card.html, templates/partials/price_table.html, templates/partials/gap_display.html, src/api/routes/dashboard.py, tests/test_dashboard_html.py</files>
  <behavior>
    - dashboard.html extends base.html and includes all partial templates
    - signal_card.html partial shows recommendation badge (color-coded: green=BUY, gray=HOLD, red=SELL), confidence bar (0-100%), reasoning text, mode toggle (Saver/Trader), and gap value
    - price_table.html partial shows responsive table with columns: Dealer, SJC Bar (Buy/Sell/Spread), Ring Gold (Buy/Sell/Spread), Last Updated
    - gap_display.html partial shows gap_vnd value, gap_pct with arrow indicator (up/down based on comparison to MA), and links to gap chart
    - All partials are HTMX-powered: rendered server-side and swapped via hx-swap="innerHTML"
    - Add HTMX partial endpoints to dashboard.py:
      - GET /dashboard/partials/signal?mode=saver — renders signal_card.html with data
      - GET /dashboard/partials/prices — renders price_table.html with data
      - GET /dashboard/partials/gap — renders gap_display.html with data
    - Dashboard loads initial data server-side (first request renders full page)
    - HTMX polling: signal and prices partials refresh every 30 seconds via hx-trigger="every 30s"
    - Mobile responsive: table scrolls horizontally on small screens, cards stack vertically
    - Tests verify: dashboard GET / returns 200, partial endpoints return HTML fragments containing expected data
  </behavior>
  <action>
    Create the dashboard layout and HTML partials:

    1. **`templates/dashboard.html`** — extends base.html, defines:
    - Page title: "Gold Advisor Vietnam — Dashboard"
    - Top section: signal card (full width)
    - Middle section: two columns on desktop (gap display left, dealer price table right), stacks on mobile
    - Bottom section: chart placeholders (filled by Task 2)
    - HTMX initialization: partials loaded via hx-get with hx-trigger="load"
    - Mode toggle: two buttons (Saver/Trader) that update signal partial with new mode param

    2. **`templates/partials/signal_card.html`** — receives context: `{signal: dict, gap: dict}`
    - Large recommendation badge: BUY (green bg), HOLD (amber bg), SELL (red bg) — Tailwind colors
    - Confidence bar: visual progress bar (0-100%) with percentage text
    - Reasoning text in italic below
    - Mode toggle buttons with active state styling
    - Gap info: "Gap: X,XXX,XXX VND (X.X%)" with trend arrow

    3. **`templates/partials/price_table.html`** — receives context: `{dealers: list}`
    - Responsive HTML table with Tailwind classes
    - Each row: dealer name (bold), SJC Bar buy/sell/spread columns, Ring Gold buy/sell/spread columns
    - Spread column: color-coded (low spread = green, high spread = amber)
    - Last updated timestamp
    - Empty state: "No price data available"
    - On mobile: horizontal scroll wrapper with `overflow-x-auto`

    4. **`templates/partials/gap_display.html`** — receives context: `{gap: dict}`
    - Large gap value with VND formatting
    - Percentage with trend arrow (compare to ma_7d if available)
    - 7d MA and 30d MA values shown as reference
    - Empty state when no gap data

    5. **Update `src/api/routes/dashboard.py`** — add 3 HTMX partial endpoints:
    - Each endpoint fetches data (same as JSON endpoints), passes to Jinja2Templates, returns TemplateResponse
    - Import `Jinja2Templates(directory="templates")` and use `templates.TemplateResponse(request, "partials/signal_card.html", context={...})`
    - Handle errors gracefully: return empty-state partials instead of 500

    6. **Update root route GET /** in main.py to pass initial data to dashboard.html

    7. **Tests in `tests/test_dashboard_html.py`**:
    - Test partial endpoints return 200 and contain expected HTML elements
    - Test signal partial contains recommendation text
    - Test price partial contains dealer names
    - Test root route returns dashboard HTML with all section containers

    Commits:
    - `test(05-02): add failing tests for dashboard HTML partials`
    - `feat(05-02): add dashboard HTML sections with HTMX partials`
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_html.py -x -v</automated>
  </verify>
  <done>
    - GET / returns full dashboard HTML with signal card, price table, gap display sections
    - HTMX partial endpoints return HTML fragments with real data
    - Signal card shows recommendation, confidence, reasoning, mode toggle
    - Price table shows all dealers with SJC bar and ring gold prices
    - Gap display shows current gap value and trend
    - Mobile responsive layout works at 375px width
    - All existing tests pass
    - 2 atomic TDD commits
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chart.js price charts + gap chart + HTMX live refresh</name>
  <files>templates/partials/price_chart.html, templates/partials/gap_chart.html, static/js/charts.js, static/js/dashboard.js, templates/dashboard.html, tests/test_dashboard_html.py</files>
  <behavior>
    - Price chart renders line chart with 3 datasets: SJC Bar (gold line), Ring Gold (amber line), International Gold (blue line)
    - Price chart has timeframe selector buttons: 1D, 1W, 1M, 1Y (default: 1M)
    - Gap chart renders line chart with gap_pct as main line and ma_7d/ma_30d as reference lines
    - Gap chart has timeframe selector: 1W, 1M, 3M, 1Y (default: 1W)
    - Charts are responsive (resize with window)
    - Charts use dark theme (dark background, light grid lines, gold accent colors) matching base template
    - Price chart data fetched from GET /api/prices/history via fetch()
    - Gap chart data fetched from GET /api/gap/history via fetch()
    - When timeframe button is clicked, chart re-fetches data and updates
    - dashboard.js handles HTMX polling setup and mode toggle click handler
    - Tests verify chart HTML partials render with canvas elements and correct data attributes
  </behavior>
  <action>
    Add Chart.js charts and interactive JavaScript:

    1. **`templates/partials/price_chart.html`** — chart container partial:
    - `<canvas id="priceChart"></canvas>` wrapped in a card container
    - Timeframe selector buttons (1D, 1W, 1M, 1Y) with active state styling
    - Data attributes for chart configuration: `data-product-types='["sjc_bar","ring_gold","xau_usd"]'`
    - Legend showing color codes for each line

    2. **`templates/partials/gap_chart.html`** — gap chart container partial:
    - `<canvas id="gapChart"></canvas>` wrapped in a card container
    - Timeframe selector buttons (1W, 1M, 3M, 1Y)
    - Legend showing gap line, 7d MA, 30d MA

    3. **`static/js/charts.js`** — Chart.js configuration:
    - `initPriceChart(canvasId, dataUrl, defaultRange)` — creates Chart.js line chart
      - 3 datasets with colors: SJC=#D4AF37 (gold), Ring=#F5D76E (light gold), International=#4A90D9 (blue)
      - Dark theme: backgroundColor='rgba(30,30,30,1)', grid lines 'rgba(255,255,255,0.1)'
      - Responsive: maintainAspectRatio=false
      - Tooltip with VND formatting for prices
    - `initGapChart(canvasId, dataUrl, defaultRange)` — creates gap percentage line chart
      - 3 datasets: gap_pct=#D4AF37, ma_7d=#888 (gray dashed), ma_30d=#666 (darker gray dashed)
      - Y-axis label "Gap (%)"
    - `fetchChartData(url)` — fetches JSON from API, handles errors gracefully
    - Timeframe button click handlers: re-fetch data, call chart.update()
    - `updateChartColors(isDark)` — apply dark/light theme colors (always dark for now)
    - Data parsing: API returns `{x, y}` format — map to Chart.js `{x: label, y: value}`

    4. **`static/js/dashboard.js`** — HTMX and interaction wiring:
    - Mode toggle handler: when Saver/Trader button clicked, reload signal partial with new mode param
    - HTMX polling is already configured via hx-trigger attributes in HTML (no extra JS needed for basic polling)
    - Error handling: show toast/notification on failed partial refresh (use a simple div that fades in/out)
    - Initialize all charts on DOMContentLoaded

    5. **Update `templates/dashboard.html`**:
    - Include chart partials in bottom section
    - Include chart.js and dashboard.js in {% block scripts %}
    - Charts in two-column layout on desktop, stacked on mobile
    - Initialize charts: `initPriceChart('priceChart', '/api/prices/history', '1M')` etc.

    6. **Update `tests/test_dashboard_html.py`**:
    - Test chart partials contain canvas elements with correct IDs
    - Test chart partials contain timeframe buttons
    - Test dashboard.html includes script tags for charts.js and dashboard.js
    - Test static JS files exist and are non-empty

    Commits:
    - `test(05-02): add failing tests for chart partials and JS`
    - `feat(05-02): add Chart.js price and gap charts with interactive controls`
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_html.py -x -v</automated>
  </verify>
  <done>
    - Price chart renders with 3 datasets (SJC, Ring, International)
    - Gap chart renders with gap percentage and moving average lines
    - Timeframe buttons re-fetch data and update charts
    - Charts use dark theme matching dashboard
    - Charts resize responsively with window
    - dashboard.js handles mode toggle correctly
    - All existing tests pass
    - 2 atomic TDD commits
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x` — all tests pass
- `curl -s http://localhost:8000/ | grep -c "signal-card\|price-table\|gap-display\|priceChart\|gapChart"` — all sections present
- `curl -s http://localhost:8000/dashboard/partials/signal | grep -c "BUY\|HOLD\|SELL"` — signal recommendation visible
- `curl -s http://localhost:8000/dashboard/partials/prices | grep -c "DOJI\|SJC\|PNJ\|BTMC\|PHUQUY"` — dealer names visible
- Manual: Open browser at localhost:8000, verify all sections render, mode toggle works, charts display
</verification>

<success_criteria>
- Complete dashboard page loads at / with all 5 sections: signal card, price table, gap display, price chart, gap chart
- HTMX partials refresh prices and signal every 30 seconds
- Chart.js renders price and gap charts with interactive timeframe selection
- Mobile responsive layout works on 375px width
- Mode toggle (Saver/Trader) updates signal display
- All project tests pass (200+)
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-dashboard/05-02-SUMMARY.md`
</output>
